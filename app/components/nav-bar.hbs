{{!-- <div class="md:text-base flex items-center justify-around text-xs text-white bg-black">
  <LinkTo @route="home" class="hover:bg-grey-dark flex-1 py-4 text-center">Home</LinkTo>
  <LinkTo @route="treatment" class="hover:bg-grey-dark flex-1 py-4 text-center">Treatment</LinkTo>
  <LinkTo @route="contact" class="hover:bg-grey-dark flex-1 py-4 text-center">Contact</LinkTo>
</div> --}}

<div class="font-narrow flex flex-wrap items-center justify-between border-b-2 border-pink-600">
  <div class="lg:hidden block">
    <button {{on "click" this.toggle}} class="hover:text-pink-300 hover:border-white flex items-center px-3 py-2 my-1 mr-2 border border-pink-400 rounded" type="button">
      {{inline-svg "assets/svg/menu" class="w-3 h-3 fill-current"}}
    </button>
  </div>
  <div class="lg:flex lg:items-center lg:w-auto relative flex-grow block w-full text-lg">
    <button class="absolute w-full bg-black border-pink-600 border-b-2 pb-4
      opacity-85 lg:opacity-100 lg:border-none lg:pb-0 lg:flex lg:flex-grow
      lg:justify-around {{this.menuClass}}" {{on "click" this.toggle}} type="button">
      <NavBarLink @route="home">Home</NavBarLink>
      <NavBarLink @route="treatment">Treament</NavBarLink>
      <NavBarLink @route="contact">Contact</NavBarLink>
    </button>
  </div>
</div>